info:
  name: Ouvrir un panier
  type: http
  seq: 7

http:
  method: POST
  url: /carts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
