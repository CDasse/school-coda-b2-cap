info:
  name: Liste des tickets
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/tickets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
