info:
  name: Obtenir un panier
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/carts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
