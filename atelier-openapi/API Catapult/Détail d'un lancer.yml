info:
  name: Détail d'un lancer
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/trips/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
